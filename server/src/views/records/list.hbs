<h1>Records for: {{ object.API_Name }}</h1>
<div class="mb-3 mt-3"></div>
    <a href="/record/add/{{objectId}}" class="btn btn-info">Create new record</a>
</div>
{{#if fields}}
  <table class="table text-center">
    <thead>
      <tr>
        {{#each fields}}
          <th>{{name}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each records}}      
        <tr>        
          {{#each values}}
            <td>{{value}}</td>
          {{/each}}
        </tr>        
      {{/each}}
    </tbody>
  </table>  
{{else}}
  <h2>No Fields</h2>
{{/if}}
